
{% extends 'base.html.twig' %}

{% block stylesheets %}
<link rel="stylesheet" href="{{asset('css/menu.css')}}">
{% endblock %}

{% block menu %}

{{ form_start(form) }}
{{ form_widget(form) }}
<div class="nav navbar-right">
	<a id="registration"  class="btn btn-default" href="/registration"><span class="glyphicon glyphicon-user"></span> Inscription</a>
</div>

{{ form_end(form) }}
{% endblock %}

{% block body %}
	<h1 class="carousel-caption">Welcome to Skeleton Online</h1>
	<div id="myCarousel" class="carousel slide" data-ride="carousel">
		<!-- Indicators -->
		<ol class="carousel-indicators">
			<li data-target="#myCarousel" data-slide-to="0" class="active"></li>
			<li data-target="#myCarousel" data-slide-to="1"></li>
			<li data-target="#myCarousel" data-slide-to="2"></li>
		</ol>
		<!-- Wrapper for slides -->
	 <div class="carousel-inner" role="listbox">
	   <div class="item active">
		 <img src="{{asset('images/Necromancy.jpg')}}" alt="Necromancy">
		 <div class="carousel-caption">
			<h4>Necromancien</h4>
		 </div>
	   </div>

	   <div class="item">
		 <img src="{{asset('images/skeletonWarrior.jpg')}}" alt="skeleton-warrior">
		 <div class="carousel-caption">
			<h4>Guerrier Squelette</h4>
		 </div>
	   </div>

	   <div class="item">
		 <img src="{{asset('images/skeletonMage.jpg')}}" alt="skeleton mage">
		 <div class="carousel-caption">
			 <h4>Mage squelette</h4>
		 </div>
	   </div>

	   <!-- Left and right controls -->
	  <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
	    <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
	    <span class="sr-only">Previous</span>
	  </a>
	  <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
	    <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
	    <span class="sr-only">Next</span>
	  </a>

	 </div>
	</div>

{% endblock %}

{% block javascripts %}
<script>
	console.log("k");

	$("#button_building").click(function()
	{
		console.log("button build");
		var email = $("#input_email").val();
		var password = $("#input_password").val();

		console.log(email);
		console.log(password);

	});


	$("#button_registration").click(function()
	{
		console.log("button_registration");
		var email = $("#input_email").val();
		var password = $("#input_password").val();
		console.log(email);
		console.log(password);

	});

	$("#button_login").click(function()
	{
		console.log("button login");
		var email = $("#input_email").val();
		var password = $("#input_password").val();
		console.log(email);
		console.log(password);
		document.location.href = "http://localhost:8000/login";
	});




</script>

{% endblock %}
